const myHeading = document.querySelector('h1');
myHeading.textContent = 'Hello world!';

// the function  is called query selector
// which passed the parameter h1 to grab a reference to
// the header and stored it in a variable called myHeading
//similar to using a selector in CSS -> to do something
// to an element you must select it first
// then it set the value of the variables textproperty 
// to hello world.